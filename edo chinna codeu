#include <stdio.h>
#include <string.h>

int main() {
    char a[15],*p;
    printf("Enter: ");
    scanf("%s",a);
     int l = strlen(a);
   p = a;
   for(int i=0;i<l;i++){
       for(int j=i;j<l;j++){
     printf("%c",*(p+j-i));
       }
       printf("\n");
}
    return 0;
}
